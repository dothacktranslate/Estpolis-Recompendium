#pragma once

/*
 * Minimal compatibility header for the raw Estpolis Denki I bootstrap probe.
 *
 * The shared SNESRecomp hardware runner includes "variables.h", but the
 * generic automatically-generated Estpolis Denki I project does not provide one.
 *
 * Intentionally empty unless the compiler demonstrates that the generic
 * hardware model requires a declaration here.
 */
